# Robot Project

📄 **Teacher Instructions (PDF):**
[Here](main.pdf)
# We Pick Project A

- **PyTorch** for training neural networks
- **CasADi** for formulating and solving optimal control problems
- **L4CasADi** to embed PyTorch neural networks inside CasADi optimization problems

```bash
pip install torch
pip install casadi
pip install l4casadi